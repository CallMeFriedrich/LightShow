fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
numpy==2.2.1
websockets==14.1
httpx==0.28.1
pyyaml==6.0.2
pillow==11.1.0
# SendSpin-Audio-Capture (natives MASS-Protokoll).
# WICHTIG: Version muss zur MASS-Version passen — MASS 2.9.x nutzt aiosendspin 6.0.5.
# Wir bieten PCM an (kein Decode) → av nicht nötig.
aiosendspin==6.0.5
